#!/bin/bash

set -eu

. .envrc

git clone \
  --depth 1 \
  --branch ${GHPAGES_BRANCH} \
  https://${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY} \
  public

hugo -t ${TEMPLATE}
cd public
git add .
git commit -m "Automated build at `date --iso-8601=seconds`"
git push
